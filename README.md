# Financial Assistant (Bank Account Manager)

Цей проєкт є частиною лабораторної роботи з дисципліни "Інструментальні засоби розробки ПЗ". Програма реалізує клас для управління банківським рахунком з підтримкою мультивалютності, історії транзакцій та системи безпеки (заморожування рахунку).

## 📋 Функціональність

Клас `BankAccount` підтримує наступні операції:

* **Базові операції:** Створення рахунку, поповнення (`deposit`) та зняття коштів (`withdraw`).
* **Мультивалютність:** Підтримка різних валют (UAH, USD, EUR тощо).
* **Перекази:** Безпечний переказ коштів між рахунками однієї валюти (`transfer`).
* **Безпека:** Можливість заморожування (`freeze`) та розморожування рахунку. Заморожений рахунок не дозволяє проводити фінансові операції.
* **Історія:** Логування всіх транзакцій (дата, тип операції, сума, баланс).
* **Депозити:** Нарахування відсотків на залишок (`apply_interest`).

## 🛠 Структура проєкту

```text
.
├── finance_manager.py       # Основний код класу BankAccount
├── tests/                   # Папка з юніт-тестами
│   └── test_finance_manager.py
└── README.md                # Опис проєкту
````

## 🚀 Як використовувати

Програма написана на **Python 3**. Додаткові бібліотеки не потрібні (використовуються лише стандартні модулі).

### Приклад коду:

```python
from finance_manager import BankAccount

# 1. Створення рахунку
my_acc = BankAccount("Олександр", 1000.0, "UAH")

# 2. Поповнення
my_acc.deposit(500)

# 3. Виведення інформації
print(my_acc)
# Результат: Рахунок: Олександр | Баланс: 1500.0 UAH | Статус: Активний
```

## 🧪 Тестування (Unit Tests)

Проєкт покритий юніт-тестами (понад 20 тестів), що перевіряють:

1.  Успішне виконання операцій.
2.  Граничні випадки (нульові суми, недостатній баланс).
3.  Обробку помилок (переказ між різними валютами, спроба зняття з замороженого рахунку).

### Запуск тестів

Для запуску всіх тестів виконайте команду в терміналі з кореневої папки проєкту:

```bash
python -m unittest discover tests
```

Або для детального виводу кожного тесту:

```bash
python -m unittest discover tests -v
```
## 📚 Documentation

Повна документація коду генерується автоматично за допомогою Doxygen та GitHub Actions.

👉 **[Переглянути документацію проєкту](https://marchenkonataliia.github.io/SDT/index.html)**
