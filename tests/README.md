# Unit Tests for Financial Assistant

Ця директорія містить набір юніт-тестів для перевірки функціональності класу `BankAccount`. Тестування забезпечує стабільність роботи фінансових операцій та коректну обробку помилок.

## 🛠 Технічні деталі

* **Фреймворк:** `unittest` (стандартна бібліотека Python).
* **Покриття коду:** Тести охоплюють понад 20 сценаріїв, включаючи успішні операції, граничні випадки (edge cases) та обробку винятків.

## 📂 Структура тестів

Тестовий набір у файлі `test_finance_manager.py` поділений на логічні блоки:

### 1. Ініціалізація рахунку
* Перевірка коректного створення рахунку (баланс, валюта, власник).
* Перевірка заборони створення рахунку з від'ємним балансом.
* Тестування текстового представлення об'єкта (`__str__`).

### 2. Поповнення (Deposit)
* Успішне поповнення балансу.
* **Граничні випадки:** Спроба поповнення на від'ємну або нульову суму.
* **Безпека:** Спроба поповнення замороженого рахунку.

### 3. Зняття коштів (Withdraw)
* Успішне зняття коштів.
* **Помилки:** Спроба зняття суми, більшої за залишок (Overdraft).
* Спроба зняття від'ємної суми.
* **Безпека:** Спроба зняття із замороженого рахунку.

### 4. Перекази (Money Transfer)
* Успішний переказ коштів між рахунками.
* **Валідація:** Перевірка відповідності валют (заборона переказу між UAH та USD).
* Перевірка типу даних отримувача.
* Атомарність операції (гроші не зникають, якщо транзакція перервана помилкою).

### 5. Управління статусом (Freeze/Unfreeze)
* Перевірка блокування рахунку (статус `is_frozen`).
* Перевірка відновлення роботи після розморожування.

### 6. Додатковий функціонал
* **Відсотки:** Коректність нарахування відсотків на залишок.
* **Історія:** Перевірка логування транзакцій (чи записуються дії "Поповнення", "Зняття" в історію).

## 🚀 Як запустити тести

Виконайте команду з **кореневої папки проєкту**:

```bash
python -m unittest discover tests -v
````

Прапор `-v` (verbose) дозволяє бачити детальний звіт по кожному окремому тесту.

-----

**Примітка:** Усі тести є незалежними один від одного. Перед кожним тестом метод `setUp()` створює нові екземпляри рахунків, щоб гарантувати чистоту експерименту.
